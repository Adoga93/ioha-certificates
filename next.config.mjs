/** @type {import('next').NextConfig} */
const nextConfig = {
  // Re-enabled strict type checking and ESLint rules to ensure the codebase
  // is thoroughly protected from regressions during all future deployments.
};

export default nextConfig;
